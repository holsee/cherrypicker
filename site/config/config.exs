import Config

# Selects the Lumis engine for MDEx syntax highlighting. Must be set before
# deps compile; `cherry.new` scaffolds the same line into generated sites.
config :mdex_native, syntax_highlighter: :lumis
